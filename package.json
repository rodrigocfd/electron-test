{
	"name": "electron-app",
	"version": "1.0.0",
	"description": "First Electron app.",
	"main": "app/index.js",
	"scripts": {
		"start": "electron .",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"electron": "^4.0.0"
	}
}
